<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="java.util.Calendar" />
        <import type="android.view.View"/>
        <import type="androidx.databinding.ObservableField" />

        <variable
            name="callback"
            type="com.hfad.agendax.data.TaskListAdapter.TaskClickListener" />

        <variable
            name="model"
            type="com.hfad.agendax.vo.Task" />

        <variable
            name="completedTaskHeaderId"
            type="ObservableField&lt;Integer&gt;" />

        <variable
            name="isSelected"
            type="Boolean" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="7dp"
        android:layout_marginTop="10dp"
        android:orientation="vertical"
        >

        <TextView
            android:id="@+id/completed_task_label"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingVertical="5dp"
            android:visibility="@{model.uid == completedTaskHeaderId ? View.VISIBLE : View.GONE}"
            android:textSize="15sp"
            android:layout_marginBottom="5dp"
            android:textStyle="bold"
            android:gravity="center"
            android:text="Completed Tasks" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal">

            <CheckBox
                android:id="@+id/task_completed"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_marginEnd="7dp"
                android:checked="@{model.completed}"
                android:onClickListener="@{() -> callback.onTaskCompleted(model, !model.completed)}"
                android:scaleX="0.90"
                android:scaleY="0.90" />

            <androidx.cardview.widget.CardView
                android:id="@+id/task_holder"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@color/white"
                android:clickable="true"
                android:focusable="true"
                android:foreground="?android:attr/selectableItemBackground"
                android:onClick="@{() -> callback.onTaskClicked(model)}"
                app:cardUseCompatPadding="true">

                <androidx.constraintlayout.widget.ConstraintLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@{isSelected ? @color/task_selection : @android:color/transparent}"
                    android:paddingHorizontal="10dp"
                    android:paddingVertical="7dp">


                    <TextView
                        android:id="@+id/task_title"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:text="@{model.title}"
                        android:textColor="?android:colorPrimary"
                        android:textSize="17sp"
                        app:layout_constraintStart_toStartOf="parent"
                        app:layout_constraintTop_toTopOf="parent"
                        tools:text="Todays Task" />

                    <TextView
                        android:id="@+id/task_time"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="7dp"
                        android:text="12:00 am"
                        android:textSize="13sp"
                        app:layout_constraintEnd_toEndOf="@id/task_title"
                        app:layout_constraintTop_toBottomOf="@id/task_title" />

                </androidx.constraintlayout.widget.ConstraintLayout>

            </androidx.cardview.widget.CardView>

        </LinearLayout>
    </LinearLayout>

</layout>
